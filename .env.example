# Optional self-test seller fixture. Assurance checks run without these values.
PAY_TO=0x0000000000000000000000000000000000000000

# Required only when the optional paid fixture is enabled.
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=

# CDP x402 facilitator for Base Mainnet.
FACILITATOR_URL=https://api.cdp.coinbase.com/platform/v2/x402

# Base Mainnet in CAIP-2 form.
X402_NETWORK=eip155:8453

# Keep the first real test deliberately tiny.
X402_PRICE_USD=0.001

# Public Base RPC used for snapshots and settlement proof verification.
BASE_RPC_URL=https://mainnet.base.org

# ERC-8021 app attribution included in x402 settlement calldata.
BUILDER_CODE=bc_h2oqnbbh

PORT=4021

# Paid canary values are intentionally omitted. Set CANARY_EXPECT_PAY_TO,
# CANARY_EXPECT_AMOUNT, CANARY_CONFIRM, and EVM_PRIVATE_KEY only immediately
# before an explicitly approved Base Mainnet canary.
